export const metadata = {
  title: 'AWS SAA-C03 — VPC Study Guide',
  description: 'Master AWS VPC: CIDR, subnets, SG vs NACL, route tables, NAT, VPC endpoints, traffic flow, memory tricks, and official docs for the SAA-C03 exam',
}

export default function VpcLayout({ children }: { children: React.ReactNode }) {
  return children
}
