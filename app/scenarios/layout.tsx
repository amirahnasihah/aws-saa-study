export const metadata = {
  title: 'AWS SAA-C03 — Scenario Patterns',
  description: 'Common AWS architecture scenarios with anatomy notes, exam traps, tips and official docs — S3 + CloudFront, VPC flows, and more.',
}

export default function ScenariosLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
